apiVersion: v1
kind: Service
metadata:
  name: {{ include "skill-matrix.fullname" . }}
  labels:
    {{- include "skill-matrix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skill-matrix.selectorLabels" . | nindent 4 }}
